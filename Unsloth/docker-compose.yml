services:
  unsloth:
    image: unsloth/unsloth
    container_name: unsloth
    environment:
      - JUPYTER_PASSWORD=mypassword
    ports:
      - "8888:8888"
      - "8000:8000"
      - "2222:22"
    volumes:
      - ./work:/workspace/work
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    restart: unless-stopped
